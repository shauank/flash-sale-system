# Grafana dashboard

Import `flash-sale-system.json` into Grafana and select the configured Prometheus
data source if prompted.

The dashboard covers the flash-sale business counters and timers, HTTP request
metrics, JVM/process metrics, Tomcat threads, and HikariCP connection pools.
